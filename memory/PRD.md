# نظام إدارة طلبات المواد - PRD

## المشكلة الأصلية
نظام لمدير مشتريات يتيح لـ 10 مشرفين مواقع إنشاء طلبات مواد، ثم يعتمدها 6 مهندسين، وأخيراً يصول الطلب لمدير المشتريات لإصدار أمر الشراء للموردين.

---

## 🚀 التحول الكامل إلى PostgreSQL (13 يناير 2026)

### ✅ ما تم إنجازه بالكامل:

#### Backend - قاعدة البيانات
- [x] اتصال مع PlanetScale PostgreSQL
- [x] إنشاء 15 جدول SQL
- [x] 59 API محولة إلى PostgreSQL
- [x] جميع الـ Routes تعمل وتم اختبارها

#### Frontend - الواجهة
- [x] `AuthContext.js` - تحديث للـ PostgreSQL APIs
- [x] `LoginPage.js` - تحديث URLs
- [x] `ProcurementDashboard.js` - تحديث fetchData و Reports
- [x] `SupervisorDashboard.js` - تحديث fetchData
- [x] `EngineerDashboard.js` - تحديث fetchData
- [x] `GeneralManagerDashboard.js` - تحديث كامل للـ GM APIs

---

## 🆕 دور مدير النظام (System Admin) - ديسمبر 2025

### ✅ تم إنجازه:

#### إعادة هيكلة الصلاحيات
- [x] إنشاء دور `system_admin` الجديد
- [x] نقل صلاحيات إدارة المستخدمين إلى مدير النظام فقط
- [x] نقل صلاحيات النسخ الاحتياطي/الاستعادة إلى مدير النظام
- [x] نقل صلاحيات تنظيف البيانات إلى مدير النظام
- [x] إزالة هذه الصلاحيات من مدير المشتريات

#### لوحة تحكم مدير النظام (`SystemAdminDashboard.js`)
- [x] إحصائيات النظام (المستخدمين، المشاريع، الموردين، الطلبات، أوامر الشراء، إجمالي المبالغ)
- [x] تبويب المستخدمين: إضافة/تعديل/حذف المستخدمين
- [x] تبويب إعدادات الشركة:
  - شعار الشركة
  - اسم الشركة
  - البريد الإلكتروني
  - رقم الهاتف
  - لون التقارير الأساسي
  - عنوان الشركة
  - رأس التقرير (Header)
  - تذييل التقرير (Footer)
- [x] تبويب النسخ الاحتياطي:
  - تصدير النسخة الاحتياطية (JSON)
  - استعادة البيانات من نسخة احتياطية
  - تنظيف جميع البيانات

#### APIs مدير النظام (`pg_sysadmin_routes.py`)
- [x] `GET /api/pg/sysadmin/stats` - إحصائيات النظام
- [x] `GET /api/pg/sysadmin/company-settings` - جلب إعدادات الشركة
- [x] `PUT /api/pg/sysadmin/company-settings` - تحديث إعدادات الشركة
- [x] `POST /api/pg/sysadmin/company-logo` - رفع شعار الشركة
- [x] `GET /api/pg/sysadmin/backup` - تصدير نسخة احتياطية
- [x] `POST /api/pg/sysadmin/restore` - استعادة من نسخة احتياطية
- [x] `POST /api/pg/sysadmin/clean-data` - تنظيف البيانات
- [x] `DELETE /api/pg/sysadmin/delete-order/{order_id}` - حذف أمر شراء

---

## 🧪 نتائج الاختبار:
| الصفحة | الحالة | البيانات |
|--------|--------|----------|
| تسجيل الدخول | ✅ | PostgreSQL |
| مدير المشتريات | ✅ | بدون إدارة المستخدمين/النسخ الاحتياطي |
| مدير النظام | ✅ | كامل الصلاحيات |
| المشرف | ✅ | طلب A1 يظهر |
| المدير العام | ✅ | أمر PO-00000001 معتمد |

---

## بيانات الاختبار (PostgreSQL)

| الدور | البريد الإلكتروني | كلمة المرور |
|-------|-----------------|-------------|
| مدير النظام | admin@system.com | 123456 |
| مدير مشتريات | notofall@gmail.com | 123456 |
| المدير العام | md@gmail.com | 123456 |
| مهندس | engineer1@test.com | 123456 |
| مشرف | supervisor1@test.com | 123456 |

---

## المهام القادمة

### 🔴 P0 - أولوية عاجلة
1. ~~إصلاح زر "عرض أمر الشراء" (لا يعرض الأرقام الصحيحة)~~ ✅ تم
2. ~~إصلاح زر "الموافقة على الكل" الذي يظهر بشكل خاطئ بعد إصدار أمر الشراء~~ ✅ تم
3. ~~إضافة فلتر "بانتظار موافقة المدير العام" في لوحة مدير المشتريات~~ ✅ تم
4. تحسين فلاتر تصدير PDF (حسب المشروع، المشرف، المهندس، نوع الموافقة)

### 🟡 P1 - أولوية متوسطة  
1. PWA للموبايل
2. Price Catalog APIs (PostgreSQL)
3. اختبار شامل للتدفق الكامل
4. تفعيل Audit Trail

### 🟢 P2 - أولوية منخفضة
1. File Attachments
2. Email Notifications (SendGrid - تحتاج مفتاح API)
3. تنظيف MongoDB القديمة من `server.py`

---

## إعدادات قاعدة البيانات

### PlanetScale PostgreSQL:
```
Host: eu-central-2.pg.psdb.cloud
Port: 6432
Database: postgres
SSL: Required
```

---

## الهيكل التقني النهائي

### Backend:
```
/app/backend/
├── server.py              # FastAPI app (MongoDB + PostgreSQL)
├── database/
│   ├── __init__.py
│   ├── config.py          # PostgreSQL settings
│   ├── connection.py      # SQLAlchemy engine
│   └── models.py          # 15 SQLAlchemy models
└── routes/
    ├── pg_auth_routes.py      # Auth APIs (13)
    ├── pg_projects_routes.py  # Projects APIs (5)
    ├── pg_suppliers_routes.py # Suppliers APIs (5)
    ├── pg_budget_routes.py    # Budget APIs (8)
    ├── pg_requests_routes.py  # Requests APIs (7)
    ├── pg_orders_routes.py    # Orders APIs (10)
    ├── pg_settings_routes.py  # Settings APIs (11)
    └── pg_sysadmin_routes.py  # System Admin APIs (8) - NEW
```

### Frontend:
```
/app/frontend/src/
├── context/AuthContext.js     # Updated for /api/pg
├── pages/
│   ├── LoginPage.js           # Updated
│   ├── ProcurementDashboard.js # Updated - removed user management
│   ├── SystemAdminDashboard.js # NEW - full admin controls
│   ├── SupervisorDashboard.js  # Updated
│   ├── EngineerDashboard.js    # Updated
│   └── GeneralManagerDashboard.js # Updated
└── utils/pdfExport.js
```

---

## الأدوار والصلاحيات

| الدور | الصلاحيات |
|-------|----------|
| **مدير النظام** | إدارة المستخدمين، النسخ الاحتياطي/الاستعادة، تنظيف البيانات، إعدادات الشركة |
| **مدير المشتريات** | إدارة الموردين، الميزانيات، الكتالوج، إصدار أوامر الشراء، تصدير التقارير |
| **المدير العام** | اعتماد أوامر الشراء الكبيرة |
| **المهندس** | اعتماد/رفض طلبات المواد |
| **المشرف** | إنشاء طلبات المواد |
| **الطابعة** | طباعة أوامر الشراء |
| **متتبع التسليم** | تتبع وتسجيل التسليمات |
