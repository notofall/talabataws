# 🚀 دليل التثبيت السريع على Windows

## الطريقة الوحيدة المطلوبة: Docker Desktop

### الخطوة 1: تثبيت Docker Desktop
1. حمّل من: **https://www.docker.com/products/docker-desktop**
2. ثبّت البرنامج
3. أعد تشغيل الكمبيوتر
4. افتح Docker Desktop وانتظر حتى يعمل (الأيقونة تصبح ثابتة)

### الخطوة 2: تشغيل التطبيق
- انقر مرتين على ملف **`start.bat`**
- انتظر 5-10 دقائق (المرة الأولى فقط)

### الخطوة 3: فتح التطبيق
- افتح المتصفح على: **http://localhost:3000**
- أنشئ حساب المدير الأول من صفحة التسجيل

---

## الأوامر اليدوية (اختياري)

```cmd
# تشغيل التطبيق
docker-compose up -d --build

# إيقاف التطبيق
docker-compose down

# عرض السجلات
docker-compose logs -f

# إعادة البناء من الصفر
docker-compose down -v
docker-compose up -d --build
```

---

## حل المشاكل الشائعة

### Docker لا يعمل؟
- تأكد من تفعيل **Hyper-V** و **WSL2** في Windows
- أعد تشغيل الكمبيوتر

### التطبيق لا يفتح؟
- انتظر 2-3 دقائق بعد تشغيل `start.bat`
- تأكد من عدم وجود برنامج آخر يستخدم المنفذ 3000

### نسيت كلمة المرور؟
- احذف البيانات وابدأ من جديد:
```cmd
docker-compose down -v
docker-compose up -d --build
```
