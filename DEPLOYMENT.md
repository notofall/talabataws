# 🚀 دليل النشر والتحديث

## متطلبات التشغيل
- Docker 20.10+
- Docker Compose 2.0+
- Git

---

## 🔧 التثبيت الأول

### 1. استنساخ المشروع
```bash
git clone <repository-url>
cd <project-folder>
```

### 2. إعداد متغيرات البيئة
```bash
cp .env.example .env
nano .env  # أو أي محرر نصوص
```

**القيم المهمة في `.env`:**
```env
# عنوان الخادم الخلفي (غيّره لعنوان IP الخادم)
BACKEND_URL=http://192.168.1.100:8001

# كلمة مرور قاعدة البيانات (غيّرها للإنتاج)
POSTGRES_PASSWORD=كلمة_مرور_قوية

# مفتاح JWT السري (غيّره للإنتاج)
SECRET_KEY=مفتاح_سري_قوي_جداً
```

### 3. التشغيل
```bash
./deploy.sh
```

أو يدوياً:
```bash
docker-compose up -d --build
```

---

## 📦 التحديث

### طريقة سهلة (موصى بها)
```bash
./deploy.sh
```

### طريقة يدوية
```bash
git pull
docker-compose up -d --build
```

---

## 🛠️ أوامر مفيدة

| الأمر | الوصف |
|-------|-------|
| `./deploy.sh` | تحديث كامل (pull + build + start) |
| `./deploy.sh start` | تشغيل الخدمات |
| `./deploy.sh stop` | إيقاف الخدمات |
| `./deploy.sh restart` | إعادة تشغيل |
| `./deploy.sh logs` | عرض السجلات |
| `./deploy.sh status` | حالة الخدمات |

---

## 🌐 المنافذ الافتراضية

| الخدمة | المنفذ |
|--------|--------|
| الواجهة (Frontend) | 3000 |
| الخادم (Backend) | 8001 |
| قاعدة البيانات | 5432 |

---

## 🔒 ملاحظات الأمان للإنتاج

1. **غيّر كلمات المرور الافتراضية** في `.env`
2. **استخدم HTTPS** عبر reverse proxy (nginx/traefik)
3. **لا تكشف منفذ قاعدة البيانات** للخارج
4. **خذ نسخ احتياطية** من `postgres_data/`

---

## 🐛 حل المشاكل

### الخدمات لا تعمل
```bash
./deploy.sh logs
docker-compose ps
```

### مشاكل الاتصال بقاعدة البيانات
```bash
docker-compose exec postgres psql -U admin -d talabat_db
```

### إعادة البناء من الصفر
```bash
docker-compose down -v
docker-compose up -d --build
```

---

## 📁 هيكل الملفات

```
project/
├── backend/           # الخادم الخلفي (FastAPI)
├── frontend/          # الواجهة (React)
├── postgres_data/     # بيانات قاعدة البيانات
├── docker-compose.yml # تكوين Docker
├── deploy.sh         # سكربت النشر
├── .env              # متغيرات البيئة (تُنشأ من .env.example)
└── .env.example      # نموذج متغيرات البيئة
```
